export interface Collection {
    address: string
    description: string
    discord_url: string
    external_url: string
    image_url: string
    name: string
    slug: string
    twitter_username: string
    schema_name: string
    symbol: string
}

export interface CollectionAsset {
    token_id: string
    image_url: string
}
